/**
 * Module errors.
 *
 * @module
 */
/** The `ChainId` resource was not in the required state */
export const ECHAIN_ID = {
  code: 0,
  name: "ECHAIN_ID",
  doc: "The `ChainId` resource was not in the required state",
} as const;
